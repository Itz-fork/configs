(*) Switch to wireguard:
    - Settings > Advanced > Connection options > Tunnel protocol


(*) Turn on malware protection:
    - Settings > Advanced > Connection options > DNS settings


(*) Use WARP tunnel:
    - Settings > Advanced > Connection options > DNS settings


(*) Config:
    - Log into Zero Trust dashboard and configure av scans and http policies
